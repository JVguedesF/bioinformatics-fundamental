[project]
name = "eukaryotic-splicing-analyzer"
version = "1.0.0"
description = "Splicing dynamics analyzer for eukaryotic genes (TP53 model)"
requires-python = ">=3.10"

dependencies = [
    "biopython",
    "python-dotenv",
]